if(XATTR_FOUND)
    # include_directories(${XATTR_INCLUDE_DIR})
    # ADD_STRIGI_ANALYZER(xattr xattrthroughanalyzer.cpp)
endif()

# disabled until a good check for making sure the file is a tga
# ADD_STRIGI_ANALYZER(tga  tgathroughanalyzer.cpp)
